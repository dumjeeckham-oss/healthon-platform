# 2026-08-03 (월)

## Community Feature — Mock → Supabase 전환 완료

- `SupabaseCommunityRepository` 구현 완료 (implements `ICommunityRepository`)
- 5개 테이블: `community_posts`, `community_comments`, `community_post_likes`, `community_bookmarks`, `community_comment_likes`
- 8개 RPC 함수: increment/decrement for comment_count, like_count, bookmark_count, comment_like_count
- `communityRepositoryProvider` → `SupabaseCommunityRepository` 주입
- `debugMockCommunityRepositoryProvider` → Mock 유지 (개발용)
- `community_mapper.dart`: `CommunityPost.fromSupabase()` / `toSupabase()`, `CommunityComment.fromSupabase()` / `toSupabase()`
- `community_migration.sql`: 전체 DDL + RLS + RPC

## 좋아요 / 북마크 / 신고 / 공유 연결 완료

- Optimistic Update: 좋아요/북마크 `setState` 즉시 반영 → Provider 호출
- 신고: `community_reports` 테이블 + `reportPostProvider` / `reportCommentProvider` + 다이얼로그 (사유 선택)
- 공유: `share_plus` 패키지, 상세 화면 + 카드 양쪽 연결
- `community_post_card.dart` → `ConsumerStatefulWidget`으로 전환
- `community_detail_screen.dart`: `_showMenu(post)` 시그니처, `_sharePost()` 추가

## 파일 상태

| 파일 | 상태 |
|---|---|
| `supabase_community_repository.dart` | 528 lines, report 포함 |
| `community_provider.dart` | 898 lines, ReportReason enum + 모든 Provider |
| `community_detail_screen.dart` | 1,349 lines, 신고 다이얼로그 + 공유 |
| `community_post_card.dart` | 949 lines, ConsumerStatefulWidget |
| `comment_widgets.dart` | 532 lines |
| `comment_section.dart` | 변경 없음 |
| `comment_utils.dart` | 변경 없음 |
| `comment_like_provider.dart` | 변경 없음 |
| `community_mapper.dart` | 157 lines |
| `community_migration.sql` | 300 lines |
| `community_report_migration.sql` | 33 lines |
| `pubspec.yaml` | share_plus 추가 |
