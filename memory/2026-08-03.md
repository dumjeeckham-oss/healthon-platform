# 2026-08-03 (월)

## Community Feature — Mock → Supabase 전환 완료

- `SupabaseCommunityRepository` 구현 완료 (implements `ICommunityRepository`)
- 5개 테이블: `community_posts`, `community_comments`, `community_post_likes`, `community_bookmarks`, `community_comment_likes`
- 8개 RPC 함수: increment/decrement for comment_count, like_count, bookmark_count, comment_like_count
- `communityRepositoryProvider` → `SupabaseCommunityRepository` 주입
- `debugMockCommunityRepositoryProvider` → Mock 유지 (개발용)
- `community_mapper.dart`: `CommunityPost.fromSupabase()` / `toSupabase()`, `CommunityComment.fromSupabase()` / `toSupabase()`
- `community_migration.sql`: 전체 DDL + RLS + RPC

## 좋아요 / 북마크 / 신고 / 공유 연결 완료

- Optimistic Update: 좋아요/북마크 `setState` 즉시 반영 → Provider 호출
- 신고: `community_reports` 테이블 + `reportPostProvider` / `reportCommentProvider` + 다이얼로그 (사유 선택)
- 공유: `share_plus` 패키지, 상세 화면 + 카드 양쪽 연결
- `community_post_card.dart` → `ConsumerStatefulWidget`으로 전환
- `community_detail_screen.dart`: `_showMenu(post)` 시그니처, `_sharePost()` 추가

## 댓글 고도화 완료 (SNS 수준)

- `community_comment.dart`: `updatedAt`, `isEdited`, `CommentSortType` enum 추가
- `community_mapper.dart`: `updatedAt` 매핑 추가
- `supabase_community_repository.dart`: `loadComments(sort:)`, `loadCommentsPaged()`, `updateComment()`, `toggleCommentLike()`, `isCommentLiked()` 추가
- `community_provider.dart`: `commentSortProvider`, `updateCommentProvider`, `toggleCommentLikeProvider` 추가 + 인터페이스 업데이트
- `comment_like_provider.dart`: `setLiked()` 메서드 추가
- `comment_utils.dart`: `kCommentMaxLinesCollapsed`, `_CommentBodyCore` (AnimatedCrossFade 더보기/접기), `isEdited` 표시
- `comment_widgets.dart`: `_showEditDialog`, `_showReportDialog` (reportCommentProvider 연결), `CommentSortDropdown`
- `comment_section.dart`: `CommentSortDropdown` 추가, `_CommentListCore`에 답글 펼치기/접기 기능 (3개 이상 시 "답글 N개 보기")
- `community_comment_upgrade.sql`: `updated_at` 컬럼 + 정렬 인덱스

## 현재 파일 상태 (최종)

| 파일 | 라인 | 내용 |
|---|---|---|
| `domain/models/community_comment.dart` | 173 | updatedAt, isEdited, CommentSortType |
| `data/community_mapper.dart` | 159 | updatedAt 매핑 |
| `data/supabase_community_repository.dart` | 601 | 모든 CRUD + 정렬 + 페이징 + 좋아요 토글 |
| `presentation/providers/community_provider.dart` | 615 | 모든 Provider + interface |
| `presentation/providers/comment_like_provider.dart` | 52 | setLiked 추가 |
| `presentation/widgets/comment_utils.dart` | 321 | 더보기/접기 + 수정됨 표시 |
| `presentation/widgets/comment_widgets.dart` | 523 | 수정/신고 다이얼로그 + 정렬 드롭다운 |
| `presentation/widgets/comment_section.dart` | 350 | 답글 펼치기/접기 + 정렬 |
| `data/community_comment_upgrade.sql` | 33 | updated_at + 인덱스 |
| `presentation/screens/community_detail_screen.dart` | 1,349 | 신고 + 공유 + 좋아요/북마크 |
| `presentation/screens/community_home_screen.dart` | - | 변경 없음 |
| `presentation/widgets/community_post_card.dart` | 949 | ConsumerStatefulWidget + 좋아요/북마크/공유 |
| `data/community_migration.sql` | 300 | 초기 DDL |
| `data/community_report_migration.sql` | 33 | 신고 테이블 |
| `pubspec.yaml` | - | share_plus 추가 |
