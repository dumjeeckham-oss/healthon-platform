## 2026-08-05 — 댓글 시스템 SNS 고도화 v2 완료

### 작업 내용
댓글 기능 5대 고도화 구현 완료:

1. **@멘션** — `MentionMixin` (자동완성 오버레이), `mentionUserProvider`, `ICommunityRepository.mentionUser()` / `createMentionNotification()`, `community_notifications` 테이블
2. **댓글 이미지 첨부** — `commentImageProvider`, Composer 📷 버튼, `_AttachmentPreview` (3-col grid + full-screen viewer), max 5, Supabase Storage bucket `community-comment-images`
3. **GIF 첨부** — `GifPickerBottomSheet` (검색/트렌딩), `gifSearchProvider`, `ICommunityRepository.searchGif()`, mock Giphy 교체 가능
4. **이모지 피커** — `EmojiBottomSheet` (커스텀 TabBarView, 카테고리별), 커서 위치 기반 삽입
5. **댓글 복사** — `showCommentMoreMenu`에 복사 메뉴 추가, `Clipboard.setData`, SnackBar "댓글이 복사되었습니다"

### 수정된 파일
- `community_comment.dart` — mentions/images/gifUrl 필드 + copyWith 확장
- `community_mapper.dart` — fromSupabase/toSupabase 신규 필드 매핑
- `community_provider.dart` — ICommunityRepository 신규 메서드 4개 + Mock 구현
- `supabase_community_repository.dart` — 신규 메서드 Supabase 구현
- `comment_utils.dart` — 복사 메뉴, _CommentImageGrid, _showFullScreenImage, Clipboard
- `comment_widgets.dart` — CommentTile images/gifUrl/mentions 전달, CommentComposer → ConsumerWidget (이모지·GIF·이미지 버튼 + AttachmentPreview)
- `comment_section.dart` — emoji/gif/image picker 콜백 연결, _extractMentions

### 신규 파일
- `comment_mention_provider.dart` — MentionUserNotifier
- `comment_gif_provider.dart` — GifSearchNotifier (mock)
- `comment_image_provider.dart` — CommentImageNotifier
- `emoji_bottom_sheet.dart` — 커스텀 이모지 BottomSheet
- `gif_picker_bottom_sheet.dart` — GIF 검색 BottomSheet
- `mention_mixin.dart` — @ 입력 자동완성 Overlay Mixin

### SQL 마이그레이션
- `community_comment_upgrade.sql` v2: mentions/images/gif_url 컬럼, community_notifications 테이블 + RLS + create_mention_notification RPC
