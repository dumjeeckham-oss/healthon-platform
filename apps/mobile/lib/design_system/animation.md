# Animation

- 페이지 전환: 300ms, `cubic-bezier(0,0,.2,1)`
- 카드 등장: opacity 0→1, scale .96→1
- 나무: 2초 주기 floating + ±1° wind
- 레벨업: scale .8→1.08→1 + glow + confetti
- Reduce Motion 활성화 시 이동/스케일을 제거하고 opacity만 150ms로 전환

Lottie 파일은 200×200, 30fps, 2초 무한 반복이며 이미지 임베딩이 없는 순수 벡터입니다.
